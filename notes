- clicking on the pic in gallery and sending the imgID, rendering the page and calling drawImg(imgID);
-render the inputs and the editor buttons to set the value that were on the element previously 
-rect upon selection 
